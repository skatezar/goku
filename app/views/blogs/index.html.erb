<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title>How to get a job in finance - Blogposts</title>
</head>
<body>
<br>
<h2 class="text-center">
  How to get a job in finance - Blogposts
</h2>
<h6 class="text-center">
	Weekly blogposts - with specific & value-adding details.
</h6>

<% if current_user and current_user.admin == true %>
  <%= link_to 'Add a Blog', new_blog_path, class: 'btn btn-primary btn-dark' %>
<% end %>





<div class="ukfinancejob-container">
  <% @blogs.each do |blogpost| %>
    <div class="ukfinancejob-card">
      <div class="card">
        <div class="card-body">
          <h5 class="card-title"><%= blogpost.title %></h5>
          <br>
          
          <p class="card-text"><strong>Description:</strong> <%= blogpost.para1 %></p>
          <br>


          <%= link_to 'View', blog_path(blogpost), class: 'btn btn-primary', style: 'width: 100%' %>
          <br>
          <br>
          
          <div class="d-flex justify-content-around">	

          <% if current_user and current_user.admin == true %>
			       <%= link_to 'Edit', edit_blog_path(blogpost), class: 'btn btn-primary' %>

			       <%= button_to 'Delete', blog_path(blogpost), method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to delete this job?' }, form: { "data-remote": true } %>
          <% end %>
          </div>
          
        </div>
      </div>
    </div>
  <% end %>
</div>

</body>
</html>